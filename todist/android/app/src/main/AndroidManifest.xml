<manifest xmlns:android="http://schemas.android.com/apk/res/android">
     
    <!-- ============================================ -->
    <!-- NOTIFICATION PERMISSIONS                      -->
    <!-- ============================================ -->
    
    <!-- For Android 13+ (API 33+) - Required for runtime notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- For Android 12+ (API 31+) - Exact alarm permission -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- For Android 14+ (API 34+) - Schedule exact alarms (alternative) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- ============================================ -->
    <!-- BACKGROUND & WAKE LOCK PERMISSIONS            -->
    <!-- ============================================ -->
    
    <!-- Wake lock to show notifications when device is sleeping -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Receive boot completed to reschedule notifications after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- For Android 8+ to show notifications on lock screen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    
    <!-- Vibration for notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- ============================================ -->
    <!-- INTERNET PERMISSIONS                          -->
    <!-- ============================================ -->
    
    <!-- Internet access (required for Supabase) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- ============================================ -->
    <!-- FOREGROUND SERVICE PERMISSIONS (Android 14+) -->
    <!-- ============================================ -->
    
    <!-- For data sync and background tasks -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    
 
    
    <application
        android:label="todist"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        
        <!-- DO NOT add ScheduledNotificationAlarmReceiver - it doesn't exist -->
        <!-- Remove this line if you have it:
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationAlarmReceiver" />
        -->
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
    
</manifest>